const checkExist = () => true;
const checkNotExist = () => false;

const readLine = function(fileName) {
  return "line1\nline2\nline3\nline4\nline5";
};

const readCharacter = function(name) {
  return "hello";
};

module.exports = {
    checkExist,
    checkNotExist,
    readLine,
    readCharacter
}